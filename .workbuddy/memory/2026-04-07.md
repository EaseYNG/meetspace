# 2026-04-07 工作记录

## 扩展测试数据导入

- 创建了 `test_data_extended.sql`，追加以下数据到 meetspace_db：
  - 用户：id 11-50，共 **40 个新用户**（含原有10个，总计 **50 用户**）
  - 活动：id 200-259，共 **60 个新活动**（加上原有 13 个，总计 **73 条活动**）
    - READY(0)：32 条（报名中）
    - CLOSED(1)：14 条（已截止）
    - OVER(2)：23 条（已结束）
    - DELETED(3)：4 条（已删除）
  - 参与记录：id 2001-2150，共 **150 条新参与记录**（总计 **176 条**）
- Hibernate sequence 已更新为 5000，不会冲突

## 注意事项
- test_data.sql 文件有 BOM 字符，直接 `mysql < file` 会报错，需用 PowerShell 去 BOM 再执行，或用 MySQL Shell/Workbench 执行
- 执行 SQL 用 cmd /c 方式绕过 PowerShell 的 BOM 问题：`cmd /c '"mysql.exe" -u root -p123456 db < file.sql'`
- 数据库中原有 admin/Void (id=1) 用户保留
- 所有测试用户密码均为 `password123`（BCrypt 加密）
