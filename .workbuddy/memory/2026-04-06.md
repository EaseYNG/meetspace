# 2026-04-06 工作记录

## 完成工作

### Flutter 前端完整实现
- **pubspec.yaml**：新增 `http ^1.2.2`、`shared_preferences ^2.3.5`、`intl ^0.20.2` 依赖
- **main.dart**：重写，配置 Material3 主题（紫色 #6C63FF）和 `/login`、`/register`、`/main` 路由
- **services/token_service.dart**：JWT token 持久化存储（SharedPreferences）
- **services/api_service.dart**：封装全部后端接口（注册/登录/profile/活动增删改查/报名/推荐）
- **pages/login_page.dart**：完整登录页（表单验证 + API 调用 + token 保存）
- **pages/register_page.dart**：完整注册页（昵称/用户名/密码/确认密码四字段）
- **pages/main_shell.dart**：底部 NavigationBar 四 tab 壳（首页/广场/活动/我的）
- **pages/home_page.dart**：首页（Banner + 我参加的活动列表）
- **pages/square_page.dart**：活动广场（搜索 + 标签过滤 + 活动卡片列表）
- **pages/activity_detail_page.dart**：活动详情页（信息展示 + 报名按钮）
- **pages/activity_manage_page.dart**：活动管理（我创建/我参加 Tab）
- **pages/create_activity_page.dart**：创建/编辑活动页（日期时间选择器）
- **pages/profile_page.dart**：个人主页（查看/编辑 profile + 退出登录）

### 数据库测试数据
- **test_data.sql**：10个用户（密码均为 password123）、13个活动（READY/CLOSED/OVER 各状态）、26条参与记录

### 活动推荐建议文档
- **RECOMMENDATION.md**：三阶段推荐方案（规则推荐→协同过滤→标签推荐），含完整 Java/SQL 代码示例

## 技术规范
- 后端 API: http://localhost:8080，JWT Bearer token
- 前端主色: #6C63FF（深紫色），Material3 设计风格
- Activity status tinyint: 0=READY, 1=CLOSED, 2=OVER, 3=DELETED（数据库存整数，非字符串）

## 启动调试记录（22:48-23:03）
- **MySQL 密码**：root 用户无密码（空密码），已修改 application.yml
- **注册 Bug**：已修复 UserServiceImpl.register() 逻辑（ifPresent→新建用户）
- **test_data.sql**：修正 status 字段为 tinyint 整数（0/1/2），sequence 表名为 users_seq / activity_seq / activity_participant_seq
- **BCrypt hash 修正**：SQL 中的旧 hash `$2a$10$N9qo8uLOickgx2ZMRZoMye...` 与 password123 不匹配，已更新为正确 hash `$2a$10$ixEgPEXmvF8c02OljUhhDu...`
- **adb reverse**：真机 USB 调试需 `adb reverse tcp:8080 tcp:8080`（adb 路径：`E:\env\androidsdk\platform-tools\adb.exe`）
- **后端重启**：杀旧进程后干净启动，确认登录正常
- **数据导入结果**：10用户 + 13活动 + 26参与记录，登录测试通过
- **前端启动**：`flutter run -d 10CE9K0FSD000P6`（V2352A Android 16 手机）
